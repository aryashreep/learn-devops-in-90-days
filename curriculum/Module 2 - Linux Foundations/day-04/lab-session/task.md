# Lab Session: Searching Mastery
## Task
- Use `find` to find all `.log` files in `/var/log`.
- Use `grep` to find the word "error" in all log files.
- Search for a specific file by its size (e.g., > 10MB).
- Use `which` to find the location of the `bash` binary.

---
### ✅ Proof of Work
Save your terminal commands and results in the **`solution/`** folder.
