# Lab Session: Networking Fundamentals
## Task
- Identify your Public and Private IP addresses.
- Use `ping` to check connectivity to `google.com`.
- Use `traceroute` to see the path your packets take to reach a destination.
- Use `curl -I` to inspect the headers of a website.

---
### ✅ Proof of Work
Save your terminal results in the **`solution/`** folder.
