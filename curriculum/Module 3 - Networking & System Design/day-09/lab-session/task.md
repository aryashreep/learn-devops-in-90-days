# Lab Session: DNS Detective
## Task
- Use `dig` to find the A record of your favorite website.
- Find the Nameservers (NS records) for a domain.
- Check the TTL (Time to Live) of a DNS record.
- Add a local host entry in `/etc/hosts` to map a fake domain to `127.0.0.1`.

---
### ✅ Proof of Work
Save your DNS query results in the **`solution/`** folder.
